import React from 'react';

const aboutlayout = ({children}) => {
    return (
        <div>
            <h1>Im About Layout</h1>
            <div>{children}</div>
        </div>
    );
};

export default aboutlayout;